.playwright-mcp/
audit-*.png
*.png
!assets/*.svg

# Secretos y configuración local (nunca al repo público)
.env*
*.key
.clasp.json
settings.local.json

# Codex (config local del agente, no se versiona)
.agents/
.codex/
AGENTS.md
